spring:
  application:
    name: order-service
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ecommerce
    username: sa
    password:
  flyway:
    enabled: false
server:
  port: 9999
product:
  find:
    feign-endpoint: localhost:8080
